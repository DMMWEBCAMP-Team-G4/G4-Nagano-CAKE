<div class=container>
  <div class="row">
    <div calss="col-md-3 offset-md-1">
      <h2>ジャンル編集</h2><br>
      <%= form_with model: @genre, url: {controller: 'genres', action: 'update' }, local:true do |f| %>
        <div>ジャンル名
          <%= f.text_field :name  %>
          <%= f.submit "変更を保存する", class: "btn btn-info" %>
        </div>
      <% end %>
    </div>
  </div>
</div>