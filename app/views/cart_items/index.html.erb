<h2>ショッピングカート</h2>
<div class="row">
  <div class="col-xs-11">
      <% if @cart_items.present? %>
        <div class="pull-right margin-bottom">
            <%= link_to "カートを空にする", cart_items_path, method: :delete, class: "glyphicon glyphicon-trash btn btn-danger" %>
        </div>
        <table class="table table-hover table bordered">
          <thead>
            <tr class="info">
              <th>商品名</th>
              <th>単価(税込)</th>
              <th>数量</th>
              <th>小計</th>
              <th></th>
            </tr>
          </thead>
          
          <tbody>
            
          </tbody>
        </table>
      <% end %>
  </div>
</div>